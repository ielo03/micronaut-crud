datasources:
  default:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    driverClassName: org.h2.Driver
    username: sa
    password: ""  # ✅ Ensure this is set (even if empty)
    dialect: H2
    schema-generate: CREATE_DROP  # Ensures schema is created for tests
    driver-type: jdbc